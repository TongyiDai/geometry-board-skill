interface:
  display_name: "几何视觉画板"
  short_description: "直白易懂、克制少字、有几何编辑感的飞书画板"
  default_prompt: "Use $geometry-board to turn this content into a clear, plain-language, minimal geometric diagram with an editorial geometry feel rather than a card grid, placing each board after its matching section."

policy:
  allow_implicit_invocation: true
