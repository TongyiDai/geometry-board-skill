interface:
  display_name: "几何视觉画板"
  short_description: "点线面体、直白易懂、克制少字、有几何编辑感的飞书画板"
  brand_color: "#2F6BFF"
  default_prompt: "Use $geometry-board to turn this content into a clear, plain-language, restrained point-line-plane-volume diagram; choose cards, points, lines, or a hybrid based on the relationships, keep one focal point and Geometry Blue, then place each board after its matching section."

policy:
  allow_implicit_invocation: true
