interface:
  display_name: "几何视觉画板"
  short_description: "直白易懂、克制少字、有几何编辑感的飞书画板"
  default_prompt: "Use $geometry-board to turn this content into a clear, plain-language, minimal geometric diagram; choose cards, points, lines, or a hybrid based on the relationships, then apply one focal point, clear alignment, readable spacing, and place each board after its matching section."

policy:
  allow_implicit_invocation: true
